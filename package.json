{
  "name": "aerodynamics-simulator",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "app",
    "api"
  ],
  "scripts": {
    "dev": "bun --filter app run dev",
    "build": "bun --filter app run build && bun --filter api run build",
    "preview": "bun --filter app run preview",
    "install": "bun install"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
